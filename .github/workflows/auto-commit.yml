name: auto-commit

on:
  workflow_dispatch:
  schedule:
    - cron: "*/15 * * * *"

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Make changes
        run: |
          echo "$(date)" >> auto-file.txt

      - name: Commit and Push
        run: |
          git config --global user.name "Ankit"
          git config --global user.email "ankitkumar823089@gimal.com.com"
          git add .
          git commit -m "auto commit"
          git push
